{
    "name": "strapi-plugin-image-color-palettes",
    "version": "1.0.0",
    "description": "A Strapi plugin that extends image uploads to generate and attach a color palette to the schema when uploaded.",
    "strapi": {
        "displayName": "Image Color Palettes",
        "name": "image-color-palettes",
        "description": "A Strapi plugin that extends image uploads to generate and attach a color palette to the schema when uploaded.",
        "kind": "plugin"
    },
    "scripts": {
        "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,css,scss,md}\""
    },
    "dependencies": {
        "gm-palette": "^1.0.3",
        "prettier": "^2.8.4"
    },
    "peerDependencies": {
        "@strapi/strapi": "^4.1.5",
        "mime-types": "2.1.35"
    }
}
