{
    "name": "strapi-plugin-image-color-palette",
    "version": "1.0.0",
    "description": "A Strapi plugin that extends image uploads to generate and attach a color palette to the schema when uploaded.",
    "strapi": {
        "displayName": "Image Color Palette",
        "name": "image-color-palette",
        "description": "A Strapi plugin that extends image uploads to generate and attach a color palette to the schema when uploaded.",
        "kind": "plugin"
    },
    "scripts": {
        "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,css,scss,md}\""
    },
    "dependencies": {
        "gm-palette": "^1.0.3"
    },
    "peerDependencies": {
        "@strapi/strapi": "^4.1.5",
        "mime-types": "2.1.35"
    },
    "devDependencies": {
        "prettier": "^2.8.4"
    }
}
